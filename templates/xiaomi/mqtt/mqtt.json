{
    "id": "fk-mqtt",
    "className": "Mqtt",
    "name": "Mqtt server",
    "config": {
        "isConfig": true,
        "category": "config",
        "fields": [
            { "id": "name", "label": "Name", "type": "text", "required": true, "default": ""},
            { "id": "host", "label": "Hostname", "type": "text", "default": "localhost"},
            { "id": "port", "label": "Port", "type": "text", "default": "1880"}
        ]
    },
    "help": "Help..."
}